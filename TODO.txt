-- Thunar font size: 
.thunar { font-size: 18pt }
in config gtk3rc

-- add aseprite to extras.sh

-- add thunar kill keybind

-- add shortcuts to thunar sidebar

-- make tmux work with old ncurses, OR, update ncurses and make ranger work

-- add to kitty: jump to tab number (Ctrl Cmd num ?)

-- automate key generation for github

-- set default programs for images

-- add kitty rename tab key (Cmd Shift T)

-- add Harpoon-like tool to terminal, maybe add keybinds, add ranger compatibility

-- i3 keybinds should display a notification (i.e. kitty reload config)

-- stop nvim ~ files

-- nvim cpp preprocessor CONSTANTS are white instead of blue

-- nvim search typo should stop at last match instead of scrolling to stop of file

-- nvim ctrl + backspace delete word

-- add dunst icons?

-- add fzf exclude directories

-- rewrite i3blocks modules to be cooler (visualize RAM? what happened to disk?)

-- maybe add ft (filter tags); might be a bad idea

-- fix ranger file type programs

-- add to i3blocks: mntkey indicator

-- add installation instructions with luks steps

-- consider auto unmount for mntkey

-- ranger: indicate whether hidden files are shown

-- add status check for auto-cpufreq (possibly to i3bar)

-- auto-cpufreq pollutes journalctl

-- add retroarch configs

-- output screen keybinds

-- move key remaps away from xmodmap

-- VLC alternative?

-- add SSH to i3bar

-- add ranger functionality to nvim

-- add fzf functionality to ranger

-- add battery notifications

-- make "pipecheck" C program for bash scripting, or find the proper way to do head | tr | read

-- exclude files from root directory (rwall service)

-- consider using polybar

-- nvim "create line" command
